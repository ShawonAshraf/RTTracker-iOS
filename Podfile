# Uncomment the next line to define a global platform for your project
# platform :ios, '11.0'

target 'RT Tracker iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # pods in use
  pod 'GoogleMaps'
  pod 'Alamofire'
  pod 'PusherSwift'

  # Pods for RT Tracker iOS

  target 'RT Tracker iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RT Tracker iOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
